import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
  // Доступ к dev-серверу с телефона в той же сети. Next сверяет Origin с этим
  // списком и на каждый запрос к /_next и на рукопожатие HMR-вебсокета: если
  // хост не разрешён, сокет получает 403, dev-клиент 25 раз пробует
  // переподключиться и перезагружает страницу.
  //
  // Здесь именно шаблоны хостов: сравнение идёт по точкам, поэтому CIDR вида
  // 192.168.0.0/16 не работает — нужен 192.168.*.*. Адрес ноутбука в сети
  // меняется, так что разрешаем все локальные диапазоны сразу.
  allowedDevOrigins: [
    "10.*.*.*",
    "172.*.*.*",
    "192.168.*.*",
    "169.254.*.*",
    "*.local",
  ],
};

export default nextConfig;
